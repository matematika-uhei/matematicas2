<?xml version="1.0" encoding="UTF-8" ?>

<section xml:id="sec_indeterminacion-0-0">
	<title>Indeterminación <m>\frac{0}{0}</m></title>

	<assemblage>
		<p>
			Si para todo <m>x \neq a</m>, <m>f(x)=g(x)</m> en algún intervalo abierto que contenga <m>a</m>, entonces <me>\limite{a}{f(x)}=\limite{a}{g(x)}</me>
		</p>
	</assemblage>
		

		<p>
			Basandonos en esto podemos calcular podemos calcular límites cuando hay una indeterminación <m>\sfrac{0}{0}</m> de la siguiente forma:
		</p>

		<p>
			<ul>
			
				<li>
					<p>
						Si <m>f(x)</m> y <m>g(x)</m> son polinomios, factorizaremos numerador y denominador y cancelamos los factores comunes.
					</p>
				</li>

				<li>
					<p>
						Si en el numerador o denominador hay una diferencia con raiz cuadrada, multiplicaremos el numerador y el denominador por el conjugado de la expresión que contiene la raiz cuadrada y luego simplificaremos.
					</p>
				</li>

			</ul>
		</p>

	<example>
		<statement>
			<p>
			Calcular el siguiente límite: <m>\limite{-1}{\dfrac{x^3-2x^2+2x+5}{x^2-6x-7}}</m>
			</p>
		</statement>
		<solution>
			<p>
				Calculamos el límite.
			</p>

			<p>
				<me>\limite{-1}{\dfrac{x^3-2x^2+2x+5}{x^2-6x-7}} = \dfrac{(-1)^3-2(-1)^2+2\cdot(-1)+5}{(-1)^2-6\cdot (-1)-7} = \left[\dfrac{0}{0}\right]_{\text{Indet.}}</me> Descomponemos numerador y denominador y simplificamos: <me>\limite{-1}{\dfrac{\bcancel{(x+1)}(x^2-3x+5)}{\bcancel{(x+1)}(x-7)}} = \dfrac{(-1)^2-3\cdot(-1)+5}{ (-1)-7}=-\dfrac{9}{8}</me>
			</p>
		</solution>
	</example>
		
	<example>
		<statement>
			<p>
				Calcular el siguiente límite: <m>\limite{2}{\dfrac{x^3-5x+2}{x^3+2x^2-3x-10}}</m>
			</p>
		</statement>
		<solution>
			<p>
				<me>\limite{2}{\dfrac{x^3-5x+2}{x^3+2x^2-3x-10}}= \dfrac{2^3-5 \cdot 2+2}{2^3+2\cdot 2^2-3\cdot 2-10}= \left[\dfrac{0}{0}\right]_{\text{Indet.}}</me>
			</p>

			<p>
				<me>\limite{-1}{\dfrac{\bcancel{(x-2)}(x^2+2x-1)}{\bcancel{(x-2)}(x^2+4x+5)}} = \dfrac{2^2+2\cdot2-1}{ 2^2+4\cdot 2 +5}=\dfrac{7}{17}</me>
			</p>
		</solution>
	</example>
		
	<example>
		<statement>
			<p>
				Calcular el siguiente límite <m>\limite{1}{\dfrac{\sqrt{x+3}-2}{x^2-1}}</m>
			</p>
		</statement>
		<solution>
			<p>
				<me>\limite{1}{\dfrac{\sqrt{x+3}-2}{x^2-1}} = \dfrac{\sqrt{1+3}-2}{1^2-1} = \left[\dfrac{0}{0}\right]_{\text{Indet.}}</me>
			</p>

			<p>
				Multiplicamos por el conjugado y simplificamos.
			</p>

			<p>
				<me>\limite{1}{\dfrac{(\sqrt{x+3}-2)(\sqrt{x+3}+2)}{(x^2-1)(\sqrt{x+3}+2)}} = \limite{1}{\dfrac{x+3-4}{(x^2-1)(\sqrt{x+3}+2)}} =</me>
			</p>

			<p>
				<me>= \limite{1}{\dfrac{\bcancel{(x-1)}}{\bcancel{(x-1)}(x+1)(\sqrt{x+3}+2)}}= \dfrac{1}{2 \cdot 4}= \dfrac{1}{8}</me>
			</p>
		</solution>
	</example>
		
	<example>
		<statement>
			<p>
				Calcular el siguiente límite: <m>\limite{2}{\dfrac{x-2}{3-\sqrt{x+7}}}</m>
			</p>
		</statement>
		<solution>
			<p>
				<me>\limite{2}{\dfrac{x-2}{3-\sqrt{x+7}}} =  \dfrac{2-2}{3-\sqrt{2+7}} =  \left[\dfrac{0}{0}\right]_{\text{Indet.}}</me>
			</p>

			<p>
				<me>\limite{2}{\dfrac{(x-2)(3+\sqrt{x+7})}{(3-\sqrt{x+7})(3+\sqrt{x+7})}}= \limite{2}{\dfrac{(x-2)(3+\sqrt{x+7})}{9-(x+7)}}=</me>
			</p>

			<p>
				<me>= \limite{2}{\dfrac{\bcancel{(x-2)}(3+\sqrt{x+7})}{-\bcancel{(x-2)}}} = \dfrac{3+3}{-1}= -6</me>
			</p>
		</solution>
	</example>
		
	<example>
		<statement>
			<p>
				Hallar el valor de <m>a</m> para que este límite exista y calcularlo para ese valor de <m>a</m>. <me>\limite{-1}{\dfrac{ax^3+3x^2-2x-3}{x^2+3x+2}}</me>
			</p>
		</statement>
		<solution>
			<p>
				Calculamos el valor del límite:
			</p>

			<p>
				<m>\limite{-1}{\dfrac{ax^3+3x^2-2x-3}{x^2+3x+2}}= \dfrac{a\cdot (-1)^3+3\cdot (-1)^2-2\cdot (-1)-3}{(-1)^2+3\cdot (-1)+2}=\dfrac{-a+2}{0}</m>
			</p>

			<p>
				Para que este límite exista el numerador tiene que ser <m>0</m> para que sea indeterminado.
			</p>

			<p>
				Por lo tanto, <m>-a+2=0 \Rightarrow a= 2</m>
			</p>

			<p>
				Con este valor calculamos el límite.
			</p>

			<p>
				<m>\limite{-1}{\dfrac{2x^3+3x^2-2x-3}{x^2+3x+2}}= \limite{-1}{\dfrac{\bcancel{(x+1)}(2x^2+x-3)}{\bcancel{(x+1)}(x+2)}}=</m>
			</p>

			<p>
				<m>=\dfrac{2\cdot(-1)^2+(-1)-3}{(-1)+2}= -2</m>
			</p>
		</solution>
	</example>

</section>
